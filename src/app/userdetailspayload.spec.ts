import { Userdetailspayload } from './userdetailspayload';

describe('Userdetailspayload', () => {
  it('should create an instance', () => {
    expect(new Userdetailspayload()).toBeTruthy();
  });
});
